Knight's Move : A chess game developed in C language .
A console-based chess game written in C, using the 0x88 board representation for efficient move generation and validation.
This project implements standard chess rules and allows two players to play directly from the terminal.
